{
  "name": "deep7",
  "version": "7.2.0-alpha.0",
  "description": "Deep associative perception",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "benchmark": "for file in *.benchmark.js; do node \"$file\"; done",
    "benchmark:events": "node events.benchmark.js",
    "benchmark:memory": "node memory.benchmark.js",
    "benchmark:association": "node association.benchmark.js",
    "benchmark:lifecycle": "node lifecycle.benchmark.js",
    "benchmark:is": "node is.benchmark.js",
    "benchmark:gets": "node gets.benchmark.js",
    "benchmark:sets": "node sets.benchmark.js",
    "benchmark:track": "node track.benchmark.js",
    "benchmarkify": "node scripts/benchmarkify.js",
    "benchmark:many": "node many.benchmark.js"
  },
  "keywords": [
    "deep"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "mitata": "^1.0.34"
  },
  "dependencies": {
    "benchmarkify": "^4.0.0"
  }
}
