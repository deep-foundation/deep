- [x] association proxy
- [x] is* getters available for all associations
- [x] events emitters available for all associations
- [x] gets methods for all associations: forEach, map, filter, reduce, every, some, find, findKey, keys, values, entries, join
- [x] sets methods universally for all associations: set, add, delete, remove, push, pop, shift, unshift
- [x] track support for gets methods, after calling sets methods
- [x] many methods as universal methods: difference, intersection, symmetricDifference, union for any collections as arrays, sets, maps, objects
- [x] basic `type` and `typed` relations in `types` Memory
- [ ] track support for many (include tests benchmarks and docs of many and track improvements)
- [ ] relations, not only `type` and `typed`, but `from`, `out`, `to`, `in`, `value`, `valued`
- [ ] relations track support
- [ ] select engine with track support, basically only relational expressions, without comparison expressions
- [ ] select and, or, not support
- [ ] select comparison expressions support
- [ ] make .patch(events: Event[]) method who patch events as patch bundle to association
- [ ] make .collect getter from tracker, who return Collect ass instance, which can be called as function collect() and return events: Event[] collected from last calling.
- [ ] psudo associations, not stored inside Memories, but faked from js native structures
- [ ] select permission support based on additional select expression
- [ ] nextjs WEBSOCKET and GET ways to sync data client<=>server and server<=>server
- [ ] nextjs repo mode support
- [ ] cli repo mode support
- [ ] mapping of sql connections, all about namespaces, tables, columns, count of rows, indexes, differents of values in columns, etc.
- [ ] cli mode commands saving
- [ ] nodejs memory saving and restoring
