# Задачи по проекту Deep

Список задач по доработке и развитию проекта Deep версии 7.2.0.

## Производительность

Важная составляющая проекта - обеспечение высокой производительности при работе с большими объемами данных. Каждый компонент системы должен быть оптимизирован по скорости и затратам памяти.

### Приоритеты оптимизации

1. **Скорость доступа к данным**: минимизация времени доступа к данным через ассоциации
2. **Эффективность обработки событий**: оптимизация механизмов подписки и уведомления
3. **Управление памятью**: контроль утечек и эффективное использование памяти в долгоживущих объектах

Результаты бенчмарков для всех компонентов обязательно нужно фиксировать в соответствующих md-файлах в разделе "Производительность".

## 1. Доработка класса Association

### 1.1. Добавление геттера proxy
- [x] Реализовать геттер `.proxy` в классе `Association`, который возвращает обернутый экземпляр класса проксей
- [x] Использовать его в конструкторе
- [x] Обновить документацию с четким указанием особенностей использования `.proxy`
- [x] Написать тесты в `association.test.js` для проверки работы геттера
- [x] Добавить бенчмарк в `association.benchmark.js` для проверки производительности

### 1.2. Изменение конструктора
- [x] Изменить порядок аргументов конструктора: первый - `self`, второй - `methods`
- [x] Установить `self` в свойство `this.this` (ссылка на то, что оборачивает ассоциация)
- [x] Обновить README.md с описанием новой структуры конструктора
- [x] Написать тесты для проверки нового функционала
- [x] Добавить бенчмарк для измерения производительности нового конструктора

### 1.3. Реорганизация _proxy
- [x] `_proxy` должен быть и в экземпляре, и статическим свойством всех ассоциаций
- [x] При обращении к ключам проксированной ассоциации должен происходить поиск сначала в экземпляре, потом в статическом свойстве
- [x] Обновить README.md с описанием новой структуры доступа к ключам
- [x] Написать тесты для проверки правильности поиска ключей
- [x] Добавить бенчмарк для измерения производительности доступа к свойствам

### 1.4. Документация Association
- [x] Обновить или создать `ASSOCIATION.md` с подробным описанием новой структуры класса
- [x] Обновить README.md, добавив ссылку на документацию Association
- [x] Убедиться, что файл `association.benchmark.js` поддерживается в `benchmarkify.js`

## 2. Создание типологии (is.js)

### 2.1. Анализ существующих методов типизации
- [x] Изучить методы типа `isObject`, `isFunction` и т.д. в директории `./.temp`
- [x] Составить список всех методов проверки типов

### 2.2. Реализация файла is.js
- [x] Создать файл `is.js`
- [x] Реализовать методы проверки типов, которые берут значение из `this.this`
- [x] Экспортировать `const types: Map<string, isMethod>` - сопоставление названий типов с методами проверки
- [x] Экспортировать `const checks: Map<isMethod, string>` - инвертированная карта от `types`
- [x] Экспортировать `const order: Array<string>` - последовательность проверки типов (например, `array` проверяется до `object`)

### 2.3. Интеграция с классом Association
- [x] Добавить все методы проверки типов в `Association._proxy` в `index.js`
- [x] Убедиться, что методы доступны через экземпляры Association

### 2.4. Тестирование is.js
- [x] Написать `is.test.js` для проверки каждого типа каждым из возможных способов
- [x] Написать `is.benchmark.js` для измерения производительности
- [x] Создать документацию `IS.md` по аналогии с `EVENTS.md` и `MEMORY.md`
- [x] Обновить README.md, добавив ссылку на документацию типов
- [x] Обновить `benchmarkify.js` для поддержки `is.benchmark.js`

## 3. Реализация универсальных методов доступа (gets.js)

### 3.1. Анализ необходимых методов
- [x] Определить набор методов, не вносящих изменений в объекты (неизменяемые операции)
- [x] Методы должны работать с перебором данных и возвращать примитивы

### 3.2. Реализация файла gets.js
- [x] Создать файл `gets.js`
- [x] Реализовать методы доступа к данным, которые берут значение из `this.this`
- [x] Методы не должны порождать события и менять исходные данные

### 3.3. Интеграция с классом Association
- [x] Добавить все методы доступа в `Association._proxy` в `index.js` после `is.js`
- [x] Убедиться, что методы доступны через экземпляры Association

### 3.4. Реализация универсальных методов перебора

#### 3.4.1. forEach(callback)
- [x] Перебор элементов коллекции или свойств объекта
- [x] Сигнатура: `forEach(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.2. map(callback)
- [x] Преобразование элементов коллекции или свойств объекта
- [x] Сигнатура: `map(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.3. filter(callback)
- [x] Фильтрация элементов коллекции или свойств объекта
- [x] Сигнатура: `filter(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.4. reduce(callback, initialValue)
- [x] Свертка элементов коллекции или свойств объекта
- [x] Сигнатура: `reduce(callback(accumulator, value, key, collection), initialValue)`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.5. every(callback)
- [x] Проверка, что все элементы удовлетворяют условию
- [x] Сигнатура: `every(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.6. some(callback)
- [x] Проверка, что хотя бы один элемент удовлетворяет условию
- [x] Сигнатура: `some(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.7. find(callback)
- [x] Поиск первого элемента, удовлетворяющего условию
- [x] Сигнатура: `find(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.8. findKey(callback)
- [x] Поиск ключа первого элемента, удовлетворяющего условию
- [x] Сигнатура: `findKey(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.9. keys()
- [x] Получение всех ключей
- [x] Сигнатура: `keys()`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.10. values()
- [x] Получение всех значений
- [x] Сигнатура: `values()`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.11. entries()
- [x] Получение всех пар ключ-значение
- [x] Сигнатура: `entries()`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.12. join(separator)
- [x] Объединение элементов в строку
- [x] Сигнатура: `join(separator = ',')`
- [x] Реализовать для всех типов данных из is.js

### 3.5. Тестирование gets.js
- [x] Написать `gets.test.js` для проверки каждого метода на каждом из типов данных из `is.js`
- [x] Написать `gets.benchmark.js` для измерения производительности
- [x] Создать документацию `GETS.md` по аналогии с `EVENTS.md` и `MEMORY.md`
- [x] Обновить README.md, добавив ссылку на документацию методов доступа
- [x] Обновить `benchmarkify.js` для поддержки `gets.benchmark.js`

## 4. Реализация универсальных методов модификации (sets.js)

### 4.1. Анализ необходимых методов
- [x] Определить набор методов, вносящих изменения в объекты и коллекции
- [x] Методы должны работать с разными типами данных и вызывать соответствующие события

### 4.2. Реализация файла sets.js
- [x] Создать файл `sets.js`
- [x] Реализовать методы модификации данных, которые берут значение из `this.this`
- [x] Все методы должны генерировать универсальное событие `change(prev, next, pos?, op)`, где:
  - `prev` - предыдущее значение
  - `next` - новое значение
  - `pos` - опциональный указатель на ключ в object/array/map
  - `op` - объект `{ method: string, arguments: Arguments }` с информацией о методе и его аргументах

### 4.3. Интеграция с классом Association
- [x] Добавить все методы модификации в `Association._proxy` в `index.js` после `gets.js`
- [x] Убедиться, что методы доступны через экземпляры Association

### 4.4. Реализация универсальных методов модификации

#### 4.4.1. Методы для всех типов данных

##### 4.4.1.1. set(key, value)
- [x] Установка значения по ключу
- [x] Для примитивных типов - выбрасывать ошибку
- [x] События: `set`, `change`

##### 4.4.1.2. delete(key)
- [x] Удаление значения по ключу
- [x] Для примитивных типов - выбрасывать ошибку
- [x] События: `delete`, `change`

##### 4.4.1.3. clear()
- [x] Очистка всех значений
- [x] Для примитивных типов - выбрасывать ошибку
- [x] События: `clear`, `change`

#### 4.4.2. Методы для объектов (Object)

##### 4.4.2.1. assign(source)
- [x] Копирование свойств из объекта source
- [x] Для не-объектов - выбрасывать ошибку
- [x] События: `assign`, `change`, серия `set`

##### 4.4.2.2. defineProperty(key, descriptor)
- [x] Определение свойства с дескриптором
- [x] Для не-объектов - выбрасывать ошибку
- [x] События: `defineProperty`, `change`

#### 4.4.3. Методы для массивов (Array)

##### 4.4.3.1. push(...items)
- [x] Добавление элементов в конец массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `push`, `change`, серия `set`, `length`

##### 4.4.3.2. pop()
- [x] Удаление последнего элемента массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `pop`, `change`, `delete`, `length`

##### 4.4.3.3. shift()
- [x] Удаление первого элемента массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `shift`, `change`, серия `set`, `length`

##### 4.4.3.4. unshift(...items)
- [x] Добавление элементов в начало массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `unshift`, `change`, серия `set`, `length`

##### 4.4.3.5. splice(start, deleteCount, ...items)
- [x] Удаление/добавление элементов массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `splice`, `change`, серия `set`, серия `delete`, `length`

##### 4.4.3.6. reverse()
- [x] Обращение порядка элементов массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `reverse`, `change`, серия `set`

##### 4.4.3.7. sort(compareFn)
- [x] Сортировка элементов массива
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `sort`, `change`, серия `set`

##### 4.4.3.8. fill(value, start, end)
- [x] Заполнение элементов массива одним значением
- [x] Для не-массивов - выбрасывать ошибку
- [x] События: `fill`, `change`, серия `set`

#### 4.4.4. Методы для Map

##### 4.4.4.1. mapSet(key, value)
- [x] Установка значения по ключу в Map
- [x] Для не-Map - выбрасывать ошибку
- [x] События: `mapSet`, `change`

##### 4.4.4.2. mapDelete(key)
- [x] Удаление значения по ключу из Map
- [x] Для не-Map - выбрасывать ошибку
- [x] События: `mapDelete`, `change`

##### 4.4.4.3. mapClear()
- [x] Очистка всех элементов Map
- [x] Для не-Map - выбрасывать ошибку
- [x] События: `mapClear`, `change`

#### 4.4.5. Методы для Set

##### 4.4.5.1. setAdd(value)
- [x] Добавление значения в Set
- [x] Для не-Set - выбрасывать ошибку
- [x] События: `setAdd`, `change`

##### 4.4.5.2. setDelete(value)
- [x] Удаление значения из Set
- [x] Для не-Set - выбрасывать ошибку
- [x] События: `setDelete`, `change`

##### 4.4.5.3. setClear()
- [x] Очистка всех элементов Set
- [x] Для не-Set - выбрасывать ошибку
- [x] События: `setClear`, `change`

#### 4.4.6. Методы для WeakMap и WeakSet

##### 4.4.6.1. weakSet(key, value) / weakAdd(value)
- [x] Добавление элемента в WeakMap/WeakSet
- [x] Для не-WeakMap/не-WeakSet - выбрасывать ошибку
- [x] События: `weakSet`/`weakAdd`, `change`

##### 4.4.6.2. weakDelete(key/value)
- [x] Удаление элемента из WeakMap/WeakSet
- [x] Для не-WeakMap/не-WeakSet - выбрасывать ошибку
- [x] События: `weakDelete`, `change`

#### 4.4.7. Композитные методы

##### 4.4.7.1. merge(source)
- [x] Умное слияние с другой структурой данных
- [x] Работает для всех ссылочных типов
- [x] События: `merge`, `change`, серия соответствующих событий (`set`, `add` и т.д.)

##### 4.4.7.2. replace(newValue)
- [x] Полная замена содержимого
- [x] Работает для всех типов
- [x] События: `replace`, `change`

##### 4.4.7.3. transform(transformFn)
- [x] Применение функции трансформации к данным
- [x] Работает для всех ссылочных типов
- [x] События: `transform`, `change`, серия соответствующих событий

#### 4.4.8. Операции для коллекций

##### 4.4.8.1. upsert(key, value, createFn)
- [x] Обновление если существует, иначе создание
- [x] Работает для Object, Map, Set, Array
- [x] События: `upsert`, `change`, соответствующие события типа

##### 4.4.8.2. patch(operations)
- [x] Применение набора операций в стиле JSON Patch
- [x] Работает для Object, Array
- [x] События: `patch`, `change`, соответствующие события для каждой операции

##### 4.4.8.3. batch(operations, options)
- [x] Пакетное выполнение операций с контролем событий
- [x] Работает для всех ссылочных типов
- [x] События: `batch`, `change`, опционально группировка событий

### 4.5. Тестирование sets.js
- [x] Написать `sets.test.js` для проверки каждого метода на каждом из типов данных из `is.js`
- [x] Проверить корректность генерации событий для каждого метода
- [x] Написать `sets.benchmark.js` для измерения производительности
- [x] Создать документацию `SETS.md` по аналогии с `EVENTS.md` и `MEMORY.md`
- [x] Обновить README.md, добавив ссылку на документацию методов модификации
- [x] Обновить `benchmarkify.js` для поддержки `sets.benchmark.js`

## 5. Реализация методов работы с множествами many.js many.test.js many.benchmark.js MANY.md

### 5.1. Анализ и проектирование
- [ ] Изучить стандартные методы Set в JavaScript и их реализацию
- [ ] Определить оптимальные алгоритмы для работы с различными типами множеств (Set, Array, Object и др.)
- [ ] Спроектировать интерфейс методов для обеспечения универсальности

### 5.2. Реализация основных операций над множествами

#### 5.2.1. difference(otherSet)
- [x] Возвращает новое множество с элементами, которые есть в текущем множестве, но отсутствуют в otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.2. intersection(otherSet)
- [x] Возвращает новое множество с элементами, которые присутствуют и в текущем множестве, и в otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.3. symmetricDifference(otherSet)
- [x] Возвращает новое множество с элементами, которые есть только в одном из множеств (текущем или otherSet)
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.4. union(otherSet)
- [x] Возвращает новое множество со всеми элементами из обоих множеств (текущего и otherSet)
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.5. isDisjointFrom(otherSet)
- [x] Проверяет, что текущее множество и otherSet не имеют общих элементов
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Оптимизировать для быстрой проверки без создания промежуточных множеств

#### 5.2.6. isSubsetOf(otherSet)
- [x] Проверяет, что все элементы текущего множества содержатся в otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Оптимизировать для быстрой проверки без создания промежуточных множеств

#### 5.2.7. isSupersetOf(otherSet)
- [x] Проверяет, что текущее множество содержит все элементы otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Оптимизировать для быстрой проверки без создания промежуточных множеств

### 5.3. Интеграция с классом Association
- [x] Добавить методы работы с множествами в `Association._proxy` в `index.js`
- [x] Обеспечить доступность методов через экземпляры Association
- [ ] Реализовать корректную генерацию событий для каждой операции

### 5.4. Тестирование и документация
- [x] Написать `many.test.js` для проверки каждого метода работы с множествами
- [x] Написать `many.benchmark.js` для измерения производительности
- [x] Создать документацию `MANY.md` с описанием всех методов и примерами использования
- [x] Обновить README.md, добавив ссылку на документацию методов работы с множествами
- [x] Обновить `benchmarkify.js` для поддержки `many.benchmark.js`

## 6. Реализация ассоциативных релейшенов и жизненного цикла

### 6.1. Анализ и проектирование
- [x] Определить концепцию ассоциативных релейшенов
- [x] Спроектировать механизмы взаимодействия между ассоциациями
- [x] Разработать концепцию жизненного цикла ассоциаций

### 6.2. Расширение класса Association для управления жизненным циклом (кажется уже реализовано lifelycle)
- [ ] Добавить статическое свойство `all` (Map) для хранения всех созданных ассоциаций
- [ ] Модифицировать конструктор для вызова глобального события `new` при создании ассоциации
- [ ] Автоматически добавлять `this.this` (оригинальный предмет, не обертку) в `all` Set при инициализации
- [ ] Реализовать генерацию предупреждений, если добавление уже происходило
- [ ] Добавить метод `kill()`, доступный даже у проксированного экземпляра
- [ ] Метод `kill()` должен:
  - [ ] Генерировать глобальное событие `kill`
  - [ ] Генерировать персональное событие `kill` у экземпляра
  - [ ] Удалять экземпляр из `Association._all`
  - [ ] Генерировать предупреждение, если удаление уже происходило
- [ ] Обеспечить корректное удаление всех связей при вызове `kill()`

### 6.3. Реализация файла relations.js как куратора работы с Memory
- [ ] Создать файл `relations.js`
- [ ] Объявить и экспортировать экземпляры Memory для ассоциативных связей:
  - [ ] `froms` - для хранения отношений источников
  - [ ] `tos` - для хранения отношений назначений
  - [ ] `types` - для хранения типов ассоциаций
  - [ ] `values` - для хранения значений ассоциаций
- [ ] Использовать эти экземпляры Memory для реализации всех релейшенов
- [ ] Обеспечить связь с системой событий для отслеживания изменений в релейшенах

### 6.4. Доработка класса Memory
- [x] Расширить конструктор Memory, добавив опцию `childSetFactory`
- [ ] Опция `childSetFactory` должна быть методом-генератором для создания дочерних множеств (для обратных ссылок)
- [x] Модифицировать метод `many()`, чтобы он использовал `childSetFactory` при создании множества
- [ ] Реализовать возможность создания дочерних множеств как экземпляров Association
- [ ] Обеспечить автоматическую генерацию событий `change` при изменениях в множествах
- [x] Сохранить обратную совместимость с текущим API Memory
- [ ] Добавить обработку краевых случаев и ошибок

### 6.5. Реализация базовых релейшенов через Memory

#### 6.5.1. from
- [ ] Указывает на источник ассоциации
- [ ] Реализовать методы доступа и модификации через `froms` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `from`

#### 6.5.2. to
- [ ] Указывает на назначение ассоциации
- [ ] Реализовать методы доступа и модификации через `tos` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `to`

#### 6.5.3. type
- [ ] Указывает на тип ассоциации
- [ ] Реализовать методы доступа и модификации через `types` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `type`

#### 6.5.4. value
- [ ] Указывает на значение ассоциации
- [ ] Реализовать методы доступа и модификации через `values` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `value`
- [ ] Обеспечить преобразование строк обратно в значение через `.value`

#### 6.5.5. typed
- [ ] Получение всех ассоциаций с указанным типом
- [ ] Реализовать методы доступа через `types` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

#### 6.5.6. valued
- [ ] Получение всех ассоциаций с указанным значением
- [ ] Реализовать методы доступа через `values` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

#### 6.5.7. out
- [ ] Указывает на все ассоциации, для которых текущая ассоциация является источником
- [ ] Реализовать методы доступа через `froms` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

#### 6.5.8. in
- [ ] Указывает на все ассоциации, для которых текущая ассоциация является назначением
- [ ] Реализовать методы доступа через `tos` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

## 7. Реализация отслеживания изменений данных (track.js)

### 7.1. Введение и концепция

Методы модификации и создания новых данных в Deep выполняют действие однократно. Однако во многих сценариях необходимо отслеживать изменения исходных данных и автоматически применять трансформации к целевым данным. Модуль `track.js` решает эту задачу, предоставляя механизм для создания и обслуживания связей между источником данных и его трансформированной версией.

Принцип работы:
1. Создается ассоциация типа `Track`, которая связывает исходные данные (from) с результирующими (to)
2. Указывается функция-трекер, которая определяет, как именно изменения в исходных данных отражаются на результирующих
3. При изменении исходных данных трекер автоматически вносит соответствующие изменения в результирующие данные
4. Трекер умно применяет изменения только к затронутым элементам, а не перестраивает всю структуру данных заново

Важно, что трекер не просто копирует изменения, а преобразует их согласно указанной функции-трекеру. Например, при использовании трекера `mapTracker`, изменение в конкретном элементе исходного массива приведет к пересчету только соответствующего элемента в результирующем массиве.

Пример использования:
```javascript
// Создаем исходный и результирующий массивы
const sourceAss = new Association([1, 2, 3, 4]);
const resultAss = new Association([2, 4, 6, 8]); // результат map * 2

// Создаем трекер, который будет отслеживать изменения
const track = new Track(mapTracker((x) => x * 2));
track.from = sourceAss;
track.to = resultAss;

// Теперь при изменении sourceAss, resultAss будет автоматически обновляться
sourceAss.push(5); // resultAss автоматически получит 10 в конце
sourceAss.set(0, 10); // resultAss[0] автоматически станет 20
```

### 7.2. Анализ методов создания и модификации данных

#### 7.2.1. Методы создания новых данных (gets.js)
- [ ] `map(callback)` - Сигнатура: `(callback(value, key, collection)) => Array`
  - Не генерирует события

- [ ] `filter(callback)` - Сигнатура: `(callback(value, key, collection)) => Array`
  - Не генерирует события

- [ ] `reduce(callback, initialValue)` - Сигнатура: `(callback(accumulator, value, key, collection), initialValue) => any`
  - Не генерирует события

- [ ] `keys()` - Сигнатура: `() => Array`
  - Не генерирует события

- [ ] `values()` - Сигнатура: `() => Array`
  - Не генерирует события

- [ ] `entries()` - Сигнатура: `() => Array`
  - Не генерирует события

#### 7.2.2. Методы модификации данных (sets.js и подмодули)
- [ ] `set(key, value)` - Сигнатура: `(key, value) => Association`
  - Генерирует событие `set`
  - Генерирует событие `change`

- [ ] `delete(key)` - Сигнатура: `(key) => Association`
  - Генерирует событие `delete`
  - Генерирует событие `change`

- [ ] `clear()` - Сигнатура: `() => Association`
  - Генерирует событие `clear`
  - Генерирует событие `change`

- [ ] `add(key, value)` - Сигнатура: `(key, value) => Association`
  - Генерирует событие `add`
  - Генерирует событие `change`

- [ ] `remove(key)` - Сигнатура: `(key) => Association`
  - Генерирует событие `remove`
  - Генерирует событие `change`

- [ ] `push(...items)` - Сигнатура: `(...items) => Association`
  - Генерирует событие `push`
  - Генерирует событие `change`
  - Генерирует серию событий `set`
  - Генерирует событие `length`

- [ ] `pop()` - Сигнатура: `() => Association`
  - Генерирует событие `pop`
  - Генерирует событие `change`
  - Генерирует событие `delete`
  - Генерирует событие `length`

- [ ] `shift()` - Сигнатура: `() => Association`
  - Генерирует событие `shift`
  - Генерирует событие `change`
  - Генерирует серию событий `set`
  - Генерирует событие `length`

- [ ] `unshift(...items)` - Сигнатура: `(...items) => Association`
  - Генерирует событие `unshift`
  - Генерирует событие `change`
  - Генерирует серию событий `set`
  - Генерирует событие `length`

- [ ] `splice(start, deleteCount, ...items)` - Сигнатура: `(start, deleteCount, ...items) => Association`
  - Генерирует событие `splice`
  - Генерирует событие `change`
  - Генерирует серию событий `set`
  - Генерирует серию событий `delete`
  - Генерирует событие `length`

- [ ] `reverse()` - Сигнатура: `() => Association`
  - Генерирует событие `reverse`
  - Генерирует событие `change`
  - Генерирует серию событий `set`

- [ ] `sort(compareFn)` - Сигнатура: `(compareFn) => Association`
  - Генерирует событие `sort`
  - Генерирует событие `change`
  - Генерирует серию событий `set`

- [ ] `fill(value, start, end)` - Сигнатура: `(value, start, end) => Association`
  - Генерирует событие `fill`
  - Генерирует событие `change`
  - Генерирует серию событий `set`

- [ ] `assign(source)` - Сигнатура: `(source) => Association`
  - Генерирует событие `assign`
  - Генерирует событие `change`
  - Генерирует серию событий `set`

- [ ] `replace(newValue)` - Сигнатура: `(newValue) => Association`
  - Генерирует событие `replace`
  - Генерирует событие `change`

#### 7.2.3. Методы работы с множествами (many.js)
- [ ] `difference(otherSet)` - Сигнатура: `(otherSet) => Association`
  - Не генерирует события

- [ ] `intersection(otherSet)` - Сигнатура: `(otherSet) => Association`
  - Не генерирует события

- [ ] `symmetricDifference(otherSet)` - Сигнатура: `(otherSet) => Association`
  - Не генерирует события

- [ ] `union(otherSet)` - Сигнатура: `(otherSet) => Association`
  - Не генерирует события

### 7.3. Реализация ассоциации Track

- [ ] Создать файл `track.js` с экспортом `export const Track = new deep()`
- [ ] Определить основные методы класса Track:
  - [ ] Конструктор принимающий функцию-трекер
  - [ ] Метод `trackFromChangeEvent()` для переподписки на изменения источника
  - [ ] Метод отслеживания изменений и применения их к целевым данным
  - [ ] Метод обработки события kill для очистки ресурсов

- [ ] Реализовать подписку на события:
  - [ ] Событие 'from' для отслеживания изменения источника
  - [ ] Событие 'change' источника данных для применения трансформаций
  - [ ] События 'kill' трекера, источника и результата для освобождения ресурсов

### 7.4. Реализация функций-трекеров для основных трансформаций

- [ ] Трекер для map: `mapTracker(mapFn)`
  - [ ] Реализовать умное отслеживание изменений отдельных элементов
  - [ ] Применять функцию mapFn только к измененным элементам

- [ ] Трекер для filter: `filterTracker(filterFn)`
  - [ ] Отслеживать добавление/удаление элементов в источнике
  - [ ] Добавлять/удалять элементы в результате в соответствии с filterFn

- [ ] Трекер для reduce: `reduceTracker(reduceFn, initialValue)`
  - [ ] Переопределять все результирующее значение при любом изменении источника

- [ ] Трекеры для остальных методов создания данных из списка 7.2.1

### 7.5. Тестирование и документация

- [ ] Написать `track.test.js` для проверки каждого трекера с каждым типом данных
- [ ] Тестировать различные сценарии изменений данных:
  - [ ] Добавление элементов
  - [ ] Удаление элементов
  - [ ] Изменение элементов
  - [ ] Полная замена данных
  - [ ] Отписка при kill

- [ ] Написать `track.benchmark.js` для измерения производительности
- [ ] Создать документацию `TRACK.md` с описанием концепции и всех трекеров
- [ ] Обновить README.md, добавив ссылку на документацию track
- [ ] Обновить `benchmarkify.js` для поддержки `track.benchmark.js`

## 8. Реализация traveler.js
Сложная концепция требует постоянной согласованности в подходе и принятии решений. Предварительного формирования плана по каждому пункту и согласования его с автором концепции.

### 8.1. Анализ и проектирование
- [ ] Клонировать или изучить репозиторий https://github.com/deep-foundation/deeplinks
- [ ] Детально проанализировать файлы https://github.com/deep-foundation/deeplinks/blob/main/imports/traveler.ts и https://github.com/deep-foundation/deeplinks/blob/main/imports/minilinks.tsx
- [ ] Определить ключевые концепции и механизмы traveler для адаптации в текущем проекте
- [ ] Спроектировать архитектуру traveler.js с учетом требований событийной модели

### 8.2. Основные функциональные возможности

#### 8.2.1. Создание traveler
- [ ] Реализовать конструктор Traveler для создания экземпляров
- [ ] Обеспечить возможность инициализации traveler с начальным множеством связей
- [ ] Реализовать механизм сохранения связи с исходным множеством

#### 8.2.2. Перемещение по релейшенам
- [ ] Реализовать методы перехода по релейшенам (from, to, type, value, out, in, typed, valued)
- [ ] Обеспечить цепочечный вызов методов для построения сложных путей
- [ ] Сохранять зависимость от исходного множества при каждом переходе

#### 8.2.3. События и реактивность
- [ ] Реализовать механизм отслеживания изменений в исходном множестве
- [ ] Обеспечить автоматическое обновление результирующего множества при изменениях в исходном
- [ ] Генерировать соответствующие события при добавлении/удалении элементов в результирующем множестве

#### 8.2.4. Жизненный цикл traveler
- [ ] Реализовать метод kill() для прекращения отслеживания изменений
- [ ] Обеспечить корректное освобождение ресурсов при уничтожении traveler
- [ ] Реализовать механизм предотвращения утечек памяти при долгой работе

### 8.3. Интеграция с экосистемой Deep

#### 8.3.1. Интеграция с Association
- [ ] Обеспечить возможность создания traveler из экземпляров Association
- [ ] Интегрировать traveler с системой событий Association
- [ ] Обеспечить корректную работу traveler с проксированными объектами

#### 8.3.2. Интеграция с Memory
- [ ] Обеспечить совместимость traveler с механизмами работы Memory
- [ ] Оптимизировать производительность traveler при работе с большими множествами в Memory

### 8.4. Тестирование и документация
- [ ] Написать `traveler.test.js` для проверки всех функциональных возможностей
- [ ] Реализовать сценарии тестирования сложных цепочек переходов
- [ ] Написать `traveler.benchmark.js` для измерения производительности
- [ ] Создать документацию `TRAVELER.md` с описанием всех методов и примерами использования
- [ ] Обновить README.md, добавив ссылку на документацию traveler
- [ ] Обновить `benchmarkify.js` для поддержки `traveler.benchmark.js`

## 9. Реализация поискового движка (select.js)

### 9.1. Анализ и проектирование
- [ ] Определить структуру и синтаксис поисковых выражений (exp)
- [ ] Спроектировать механизм первичного поиска и реактивного обновления
- [ ] Разработать архитектуру класса Selection для управления результатами поиска

### 9.2. Реализация механизма первичного поиска

#### 9.2.1. Метод select
- [ ] Реализовать метод `ass.select({ /* exp */ }, resultSet?: associationSet)`
- [ ] Обеспечить создание ассоциативного множества для результатов, если не передано
- [ ] Реализовать цикл обработки ключей в exp и создания дочерних selection

#### 9.2.2. Обработка различных типов значений в exp
- [ ] Реализовать обработку пустого exp (возвращать all ассоциацию)
- [ ] Реализовать обработку ассоциаций в ключах exp (использовать соответствующее множество из memory)
- [ ] Реализовать обработку вложенных объектов exp (создавать дочерние selection)

#### 9.2.3. Соединение результатов
- [ ] Реализовать механизм intersection для соединения результатов дочерних selection
- [ ] Использовать traveler для сохранения событийной созависимости
- [ ] Обеспечить корректное применение смещений при переходе между множествами

### 9.3. Реализация механизма реактивного обновления

#### 9.3.1. Отслеживание изменений
- [ ] Реализовать механизм подписки на события change для всех компонентов selection
- [ ] Обеспечить автоматическое обновление результирующего множества при изменениях в исходных данных
- [ ] Корректно обрабатывать добавление/удаление связей в исходных множествах

#### 9.3.2. Оптимизация обновлений
- [ ] Реализовать механизм буферизации событий для предотвращения избыточных обновлений
- [ ] Оптимизировать процесс обновления для минимизации количества операций
- [ ] Обеспечить корректную работу при сложных цепочках зависимостей

### 9.4. Реализация механизма обновления запроса

#### 9.4.1. Метод обновления selection
- [ ] Реализовать метод `selection.select(/* exp */)` для обновления существующего selection
- [ ] Обеспечить корректное удаление (kill) исчезнувших в новом запросе дочерних selection
- [ ] Реализовать создание новых дочерних Selection для новых ключей

#### 9.4.2. Рекурсивное обновление
- [ ] Реализовать механизм рекурсивного обновления для вложенных selection
- [ ] Обеспечить корректную обработку изменений на всех уровнях вложенности
- [ ] Гарантировать сохранение событийной зависимости при обновлении

### 9.5. Интеграция с классом Association

#### 9.5.1. Встраивание в экосистему
- [ ] Добавить класс Selection как расширение Association
- [ ] Интегрировать методы select в `Association._proxy` в `index.js`
- [ ] Обеспечить доступность метода select через экземпляры Association

#### 9.5.2. Событийная модель
- [ ] Интегрировать Selection с общей системой событий
- [ ] Обеспечить генерацию соответствующих событий при изменениях в результатах поиска
- [ ] Использовать методы sets.js для операций с множествами для правильной эмиссии событий

### 9.6. Тестирование и документация
- [ ] Написать `select.test.js` для проверки всех функциональных возможностей
- [ ] Реализовать сценарии тестирования сложных поисковых выражений
- [ ] Написать `select.benchmark.js` для измерения производительности
- [ ] Создать документацию `SELECT.md` с описанием синтаксиса exp и всех методов
- [ ] Обновить README.md, добавив ссылку на документацию select
- [ ] Обновить `benchmarkify.js` для поддержки `select.benchmark.js`

## 10. Реализация логических операций (logic.js)

### 10.1. Анализ и проектирование
- [ ] Определить синтаксис и семантику логических операций (and, or, not)
- [ ] Спроектировать механизм интеграции логических операций с select.js
- [ ] Разработать архитектуру для обработки сложных логических выражений

### 10.2. Расширение механизма select

#### 10.2.1. Интеграция с select.js
- [ ] Расширить select.js для распознавания ключей and, or, not в exp выражениях
- [ ] Обеспечить их приоритетную обработку перед релейшен ключами
- [ ] Обеспечить корректную работу с существующими механизмами selection

#### 10.2.2. Обработка and
- [ ] Реализовать обработку and в exp выражениях
- [ ] Поддержать различные типы значений ключа and:
  - [ ] Конкретная ассоциация (дополнительная фильтрация результатов)
  - [ ] Объект exp (дополнительные условия поиска)
- [ ] Обеспечить корректную генерацию событий при изменениях

#### 10.2.3. Обработка not
- [ ] Реализовать обработку not в exp выражениях
- [ ] Обеспечить исключение (difference) результатов not из общего результата
- [ ] Реализовать проверку, что значение not является объектом exp
- [ ] Обеспечить корректную обработку событий при изменениях в исключаемом множестве

#### 10.2.4. Обработка or
- [ ] Реализовать обработку or в exp выражениях
- [ ] Обеспечить объединение (union) результатов для каждого элемента массива or
- [ ] Реализовать проверку, что значение or является массивом выражений exp
- [ ] Обеспечить корректную обработку событий при изменениях в любом из объединяемых множеств

### 10.3. Оптимизация логических операций

#### 10.3.1. Оптимизация производительности
- [ ] Реализовать механизмы кэширования промежуточных результатов
- [ ] Оптимизировать обработку сложных логических выражений
- [ ] Минимизировать количество операций при обновлении результатов

#### 10.3.2. Оптимизация памяти
- [ ] Реализовать механизмы предотвращения дублирования результатов
- [ ] Оптимизировать использование памяти при работе с большими множествами
- [ ] Обеспечить корректное освобождение ресурсов при обновлении запросов

### 10.4. Интеграция с traveler и select

#### 10.4.1. Взаимодействие с traveler
- [ ] Обеспечить корректную работу логических операций с механизмами traveler
- [ ] Сохранять событийную зависимость при применении логических операций
- [ ] Оптимизировать взаимодействие для повышения производительности

#### 10.4.2. Расширение возможностей selection
- [ ] Обеспечить доступность логических операций в цепочке методов selection
- [ ] Реализовать методы для динамического добавления логических условий
- [ ] Обеспечить корректное обновление результатов при изменении логических условий

### 10.5. Тестирование и документация
- [ ] Написать `logic.test.js` для проверки всех логических операций
- [ ] Реализовать сценарии тестирования сложных логических выражений
- [ ] Написать `logic.benchmark.js` для измерения производительности
- [ ] Создать документацию `LOGIC.md` с описанием синтаксиса и семантики логических операций
- [ ] Обновить README.md, добавив ссылку на документацию логических операций
- [ ] Обновить `benchmarkify.js` для поддержки `logic.benchmark.js`

## 11. Реализация ассоциаций деревьев

### 11.1. Анализ materialized-path
- [x] Изучить документацию и реализацию библиотеки materialized-path
- [ ] Определить ключевые концепции и механизмы для интеграции

### 11.2. Реализация релейшенов для деревьев

#### 11.2.1. up
- [ ] Получение всех родительских ассоциаций в дереве
- [ ] Реализовать методы доступа с поддержкой различных видов иерархий

#### 11.2.2. down
- [ ] Получение всех дочерних ассоциаций в дереве
- [ ] Реализовать методы доступа с поддержкой различных видов иерархий

### 11.3. Интеграция с системой релейшенов
- [ ] Интегрировать древовидные релейшены с основной системой ассоциативных релейшенов
- [ ] Реализовать механизм построения и обновления материализованных путей

### 11.4. Тестирование и документация
- [ ] Написать `trees.test.js` для проверки работы древовидных структур
- [ ] Написать `trees.benchmark.js` для измерения производительности
- [ ] Создать документацию `TREES.md`
- [ ] Обновить README.md, добавив ссылку на документацию деревьев
- [ ] Обновить `benchmarkify.js` для поддержки `trees.benchmark.js`

## 12. Реализация структурных операций

### 12.1. Анализ и проектирование
- [ ] Спроектировать универсальный механизм работы с вложенными структурами
- [ ] Определить правила обработки путей в различных контекстах

### 12.2. Реализация основных операций

#### 12.2.1. getPath(path, options)
- [ ] Получение значения по пути
- [ ] Поддержка путей в виде строки и массива
- [ ] Опции: `{ tree? }` - тип дерева для прохода (по умолчанию: нативные типы, затем contain)

#### 12.2.2. setPath(path, value, options)
- [ ] Установка значения по пути
- [ ] Поддержка путей в виде строки и массива
- [ ] Опции: `{ tree? }` - тип дерева для прохода
- [ ] События: `setPath`, `change`, соответствующие события для конечных значений

#### 12.2.3. deletePath(path, options)
- [ ] Удаление значения по пути
- [ ] Поддержка путей в виде строки и массива
- [ ] Опции: `{ tree? }`
