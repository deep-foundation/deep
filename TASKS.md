# Задачи по проекту Deep

Список задач по доработке и развитию проекта Deep версии 7.2.0.

## Производительность

Важная составляющая проекта - обеспечение высокой производительности при работе с большими объемами данных. Каждый компонент системы должен быть оптимизирован по скорости и затратам памяти.

### Приоритеты оптимизации

1. **Скорость доступа к данным**: минимизация времени доступа к данным через ассоциации
2. **Эффективность обработки событий**: оптимизация механизмов подписки и уведомления
3. **Управление памятью**: контроль утечек и эффективное использование памяти в долгоживущих объектах

Результаты бенчмарков для всех компонентов обязательно нужно фиксировать в соответствующих md-файлах в разделе "Производительность".

## 1. Доработка класса Association

### 1.1. Добавление геттера proxy
- [x] Реализовать геттер `.proxy` в классе `Association`, который возвращает обернутый экземпляр класса проксей
- [x] Использовать его в конструкторе
- [x] Обновить документацию с четким указанием особенностей использования `.proxy`
- [x] Написать тесты в `association.test.js` для проверки работы геттера
- [x] Добавить бенчмарк в `association.benchmark.js` для проверки производительности

### 1.2. Изменение конструктора
- [x] Изменить порядок аргументов конструктора: первый - `self`, второй - `methods`
- [x] Установить `self` в свойство `this.this` (ссылка на то, что оборачивает ассоциация)
- [x] Обновить README.md с описанием новой структуры конструктора
- [x] Написать тесты для проверки нового функционала
- [x] Добавить бенчмарк для измерения производительности нового конструктора

### 1.3. Реорганизация _proxy
- [x] `_proxy` должен быть и в экземпляре, и статическим свойством всех ассоциаций
- [x] При обращении к ключам проксированной ассоциации должен происходить поиск сначала в экземпляре, потом в статическом свойстве
- [x] Обновить README.md с описанием новой структуры доступа к ключам
- [x] Написать тесты для проверки правильности поиска ключей
- [x] Добавить бенчмарк для измерения производительности доступа к свойствам

### 1.4. Документация Association
- [x] Обновить или создать `ASSOCIATION.md` с подробным описанием новой структуры класса
- [x] Обновить README.md, добавив ссылку на документацию Association
- [x] Убедиться, что файл `association.benchmark.js` поддерживается в `benchmarkify.js`

## 2. Создание типологии (is.js)

### 2.1. Анализ существующих методов типизации
- [x] Изучить методы типа `isObject`, `isFunction` и т.д. в директории `./.temp`
- [x] Составить список всех методов проверки типов

### 2.2. Реализация файла is.js
- [x] Создать файл `is.js`
- [x] Реализовать методы проверки типов, которые берут значение из `this.this`
- [x] Экспортировать `const types: Map<string, isMethod>` - сопоставление названий типов с методами проверки
- [x] Экспортировать `const checks: Map<isMethod, string>` - инвертированная карта от `types`
- [x] Экспортировать `const order: Array<string>` - последовательность проверки типов (например, `array` проверяется до `object`)

### 2.3. Интеграция с классом Association
- [x] Добавить все методы проверки типов в `Association._proxy` в `index.js`
- [x] Убедиться, что методы доступны через экземпляры Association

### 2.4. Тестирование is.js
- [x] Написать `is.test.js` для проверки каждого типа каждым из возможных способов
- [x] Написать `is.benchmark.js` для измерения производительности
- [x] Создать документацию `IS.md` по аналогии с `EVENTS.md` и `MEMORY.md`
- [x] Обновить README.md, добавив ссылку на документацию типов
- [x] Обновить `benchmarkify.js` для поддержки `is.benchmark.js`

## 3. Реализация универсальных методов доступа (gets.js)

### 3.1. Анализ необходимых методов
- [x] Определить набор методов, не вносящих изменений в объекты (неизменяемые операции)
- [x] Методы должны работать с перебором данных и возвращать примитивы

### 3.2. Реализация файла gets.js
- [x] Создать файл `gets.js`
- [x] Реализовать методы доступа к данным, которые берут значение из `this.this`
- [x] Методы не должны порождать события и менять исходные данные

### 3.3. Интеграция с классом Association
- [x] Добавить все методы доступа в `Association._proxy` в `index.js` после `is.js`
- [x] Убедиться, что методы доступны через экземпляры Association

### 3.4. Реализация универсальных методов перебора

#### 3.4.1. forEach(callback)
- [x] Перебор элементов коллекции или свойств объекта
- [x] Сигнатура: `forEach(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.2. map(callback)
- [x] Преобразование элементов коллекции или свойств объекта
- [x] Сигнатура: `map(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.3. filter(callback)
- [x] Фильтрация элементов коллекции или свойств объекта
- [x] Сигнатура: `filter(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.4. reduce(callback, initialValue)
- [x] Свертка элементов коллекции или свойств объекта
- [x] Сигнатура: `reduce(callback(accumulator, value, key, collection), initialValue)`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.5. every(callback)
- [x] Проверка, что все элементы удовлетворяют условию
- [x] Сигнатура: `every(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.6. some(callback)
- [x] Проверка, что хотя бы один элемент удовлетворяет условию
- [x] Сигнатура: `some(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.7. find(callback)
- [x] Поиск первого элемента, удовлетворяющего условию
- [x] Сигнатура: `find(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.8. findKey(callback)
- [x] Поиск ключа первого элемента, удовлетворяющего условию
- [x] Сигнатура: `findKey(callback(value, key, collection))`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.9. keys()
- [x] Получение всех ключей
- [x] Сигнатура: `keys()`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.10. values()
- [x] Получение всех значений
- [x] Сигнатура: `values()`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.11. entries()
- [x] Получение всех пар ключ-значение
- [x] Сигнатура: `entries()`
- [x] Реализовать для всех типов данных из is.js

#### 3.4.12. join(separator)
- [x] Объединение элементов в строку
- [x] Сигнатура: `join(separator = ',')`
- [x] Реализовать для всех типов данных из is.js

### 3.5. Тестирование gets.js
- [x] Написать `gets.test.js` для проверки каждого метода на каждом из типов данных из `is.js`
- [x] Написать `gets.benchmark.js` для измерения производительности
- [x] Создать документацию `GETS.md` по аналогии с `EVENTS.md` и `MEMORY.md`
- [x] Обновить README.md, добавив ссылку на документацию методов доступа
- [x] Обновить `benchmarkify.js` для поддержки `gets.benchmark.js`

## 4. Реализация универсальных методов модификации (sets.js)

### 4.1. Анализ и проектирование
- [ ] Изучить текущую реализацию sets.js и выявить проблемы
- [ ] Определить базовый набор методов модификации для всех типов данных
- [ ] Спроектировать унифицированный подход к обработке типов через detect
- [ ] Разработать механизм генерации событий для всех операций

### 4.2. Реализация базовых методов модификации

#### 4.2.1. [x] set(key, value)
- [x] **Определение**: Установка значения по ключу в коллекции, объекте или примитиве.
- [x] **Сигнатура**: `set(key, value) => Association`

**Применимость по типам**:
- ✅ array: arr[index] = value
- ✅ map: map.set(key, value)
- ✅ set: set.add(value)
- ✅ object: obj[key] = value
- ✅ string: str[index] = value (установка символа по индексу)
- ✅ number: num[index] = value (установка цифры по индексу)
- ❌ boolean: не применимо
- ❌ null: не применимо
- ❌ undefined: не применимо
- ❌ symbol: не применимо
- ❌ bigint: не применимо
- ❌ function: не применимо
- ✅ weakmap: weakmap.set(key, value)
- ✅ weakset: использовать add() вместо set()

**Тестирование**:
- [x] Тест для array: установка значения по индексу
- [x] Тест для map: установка значения по ключу
- [x] Тест для set: добавление значения
- [x] Тест для object: установка свойства
- [x] Тест для string: установка символа по индексу
- [x] Тест для number: установка цифры по индексу
- [x] Тест для boolean: проверка выброса ошибки
- [x] Тест для null: проверка выброса ошибки
- [x] Тест для undefined: проверка выброса ошибки
- [x] Тест для symbol: проверка выброса ошибки
- [x] Тест для bigint: проверка выброса ошибки
- [x] Тест для function: проверка выброса ошибки
- [x] Тест для weakmap: добавление объекта и проверка ошибок
- [x] Тест для weakset: проверка использования add вместо set

**Генерация событий**:
- [x] set: при успешной установке значения
- [x] change: при любом изменении

#### 4.2.2. [x] delete(key)
- [x] **Определение**: Удаление значения по ключу из коллекции или объекта.
- [x] **Сигнатура**: `delete(key) => Association`

**Применимость по типам**:
- ✅ array: arr.splice(index, 1)
- ✅ map: map.delete(key)
- ✅ set: set.delete(key)
- ✅ object: delete obj[key]
- ❌ primitive: не применимо
- ✅ weakmap: weakmap.delete(key)
- ✅ weakset: weakset.delete(value)

**Тестирование**:
- [x] Тест для array: удаление элемента по индексу
- [x] Тест для map: удаление по ключу
- [x] Тест для set: удаление элемента
- [x] Тест для object: удаление свойства по ключу
- [x] Тест для primitive: проверка выброса ошибки
- [x] Тест для weakmap: удаление объекта и проверка ошибок
- [x] Тест для weakset: удаление объекта и проверка ошибок

**Генерация событий**:
- [x] delete: при успешном удалении
- [x] change: при любом изменении

#### 4.2.3. [ ] clear()
- [ ] **Определение**: Очистка всех значений из коллекции или объекта.
- [ ] **Сигнатура**: `clear() => Association`

**Применимость по типам**:
- ✅ array: arr.length = 0
- ✅ map: map.clear()
- ✅ set: set.clear()
- ✅ object: Object.keys(obj).forEach(key => delete obj[key])
- ❌ primitive: не применимо
- ❌ weakmap: требует обсуждения
- ❌ weakset: требует обсуждения

**Тестирование**:
- [ ] Тест для array: очистка массива
- [ ] Тест для map: очистка карты
- [ ] Тест для set: очистка множества
- [ ] Тест для object: очистка объекта
- [ ] Тест для primitive: проверка выброса ошибки
- [ ] Тест для weakmap: после согласования
- [ ] Тест для weakset: после согласования

**Генерация событий**:
- [ ] clear: при успешной очистке
- [ ] change: при любом изменении

#### 4.2.4. [x] add(value)
- [x] **Определение**: Добавление значения в коллекцию.
- [x] **Сигнатура**: `add(value) => Association`

**Применимость по типам**:
- ✅ array: arr.push(value)
- ✅ map: map.set(value, value)
- ✅ set: set.add(value)
- ✅ object: obj[Object.keys(obj).length] = value
- ❌ primitive: не применимо
- ✅ weakmap: weakmap.set(value, value) с проверкой типа объекта
- ✅ weakset: weakset.add(value) с проверкой типа объекта

**Тестирование**:
- [x] Тест для array: добавление элемента
- [x] Тест для map: добавление значения
- [x] Тест для set: добавление элемента
- [x] Тест для object: добавление свойства
- [x] Тест для primitive: проверка выброса ошибки
- [x] Тест для weakmap: добавление объекта и проверка ошибок
- [x] Тест для weakset: добавление объекта и проверка ошибок

**Генерация событий**:
- [x] add: при успешном добавлении
- [x] change: при любом изменении

#### 4.2.5. [x] remove(value)
- [x] **Определение**: Удаление значения из коллекции.
- [x] **Сигнатура**: `remove(value) => Association`

**Применимость по типам**:
- ✅ array: arr.splice(arr.indexOf(value), 1)
- ✅ map: map.delete(value)
- ✅ set: set.delete(value)
- ✅ object: delete obj[Object.keys(obj).find(k => obj[k] === value)]
- ❌ primitive: не применимо
- ❌ weakmap: требует обсуждения
- ❌ weakset: требует обсуждения

**Тестирование**:
- [x] Тест для array: удаление элемента по значению
- [x] Тест для map: удаление по значению
- [x] Тест для set: удаление элемента
- [x] Тест для object: удаление свойства по значению
- [x] Тест для primitive: проверка выброса ошибки
- [x] Тест для weakmap: после согласования
- [x] Тест для weakset: после согласования

**Генерация событий**:
- [x] remove: при успешном удалении
- [x] change: при любом изменении

### 4.3. Реализация методов для массивов

#### 4.3.1. [x] push(...items)
- [x] **Определение**: Добавление элементов в конец массива.
- [x] **Сигнатура**: `push(...items) => Association`

**Применимость по типам**:
- ✅ array: arr.push(...items)
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [x] Тест для array: добавление одного элемента
- [x] Тест для array: добавление нескольких элементов
- [x] Тест для map: проверка выброса ошибки
- [x] Тест для set: проверка выброса ошибки
- [x] Тест для object: проверка выброса ошибки
- [x] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [x] push: при успешном добавлении
- [x] change: при любом изменении
- [x] set: для каждого добавленного элемента
- [x] length: при изменении длины

#### 4.3.2. [x] pop()
- [x] **Определение**: Удаление последнего элемента массива.
- [x] **Сигнатура**: `pop() => Association`

**Применимость по типам**:
- ✅ array: arr.pop()
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [x] Тест для array: удаление последнего элемента
- [x] Тест для array: удаление из пустого массива
- [x] Тест для map: проверка выброса ошибки
- [x] Тест для set: проверка выброса ошибки
- [x] Тест для object: проверка выброса ошибки
- [x] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [x] pop: при успешном удалении
- [x] change: при любом изменении
- [x] delete: для удаленного элемента
- [x] length: при изменении длины

#### 4.3.3. [x] shift()
- [x] **Определение**: Удаление первого элемента массива.
- [x] **Сигнатура**: `shift() => Association`

**Применимость по типам**:
- ✅ array: arr.shift()
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [x] Тест для array: удаление первого элемента
- [x] Тест для array: удаление из пустого массива
- [x] Тест для map: проверка выброса ошибки
- [x] Тест для set: проверка выброса ошибки
- [x] Тест для object: проверка выброса ошибки
- [x] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [x] shift: при успешном удалении
- [x] change: при любом изменении
- [x] set: для каждого сдвинутого элемента
- [x] length: при изменении длины

#### 4.3.4. [x] unshift(...items)
- [x] **Определение**: Добавление элементов в начало массива.
- [x] **Сигнатура**: `unshift(...items) => Association`

**Применимость по типам**:
- ✅ array: arr.unshift(...items)
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [x] Тест для array: добавление одного элемента в начало
- [x] Тест для array: добавление нескольких элементов в начало
- [x] Тест для map: проверка выброса ошибки
- [x] Тест для set: проверка выброса ошибки
- [x] Тест для object: проверка выброса ошибки
- [x] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [x] unshift: при успешном добавлении
- [x] change: при любом изменении
- [x] set: для каждого сдвинутого элемента
- [x] length: при изменении длины

#### 4.3.5. [ ] splice(start, deleteCount, ...items)
- [ ] **Определение**: Удаление/добавление элементов массива.
- [ ] **Сигнатура**: `splice(start, deleteCount, ...items) => Association`

**Применимость по типам**:
- ✅ array: arr.splice(start, deleteCount, ...items)
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [ ] Тест для array: удаление элементов
- [ ] Тест для array: добавление элементов
- [ ] Тест для array: замена элементов
- [ ] Тест для map: проверка выброса ошибки
- [ ] Тест для set: проверка выброса ошибки
- [ ] Тест для object: проверка выброса ошибки
- [ ] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [ ] splice: при успешном изменении
- [ ] change: при любом изменении
- [ ] set: для каждого добавленного/измененного элемента
- [ ] delete: для каждого удаленного элемента
- [ ] length: при изменении длины

#### 4.3.6. [ ] reverse()
- [ ] **Определение**: Обращение порядка элементов массива.
- [ ] **Сигнатура**: `reverse() => Association`

**Применимость по типам**:
- ✅ array: arr.reverse()
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [ ] Тест для array: обращение непустого массива
- [ ] Тест для array: обращение пустого массива
- [ ] Тест для map: проверка выброса ошибки
- [ ] Тест для set: проверка выброса ошибки
- [ ] Тест для object: проверка выброса ошибки
- [ ] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [ ] reverse: при успешном обращении
- [ ] change: при любом изменении
- [ ] set: для каждого измененного элемента

#### 4.3.7. [ ] sort(compareFn)
- [ ] **Определение**: Сортировка элементов массива.
- [ ] **Сигнатура**: `sort(compareFn) => Association`

**Применимость по типам**:
- ✅ array: arr.sort(compareFn)
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [ ] Тест для array: сортировка чисел
- [ ] Тест для array: сортировка строк
- [ ] Тест для array: сортировка с пользовательской функцией сравнения
- [ ] Тест для map: проверка выброса ошибки
- [ ] Тест для set: проверка выброса ошибки
- [ ] Тест для object: проверка выброса ошибки
- [ ] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [ ] sort: при успешной сортировке
- [ ] change: при любом изменении
- [ ] set: для каждого измененного элемента

#### 4.3.8. [ ] fill(value, start, end)
- [ ] **Определение**: Заполнение элементов массива одним значением.
- [ ] **Сигнатура**: `fill(value, start, end) => Association`

**Применимость по типам**:
- ✅ array: arr.fill(value, start, end)
- ❌ map: не применимо
- ❌ set: не применимо
- ❌ object: не применимо
- ❌ primitive: не применимо
- ❌ weakmap: не применимо
- ❌ weakset: не применимо

**Тестирование**:
- [ ] Тест для array: заполнение всего массива
- [ ] Тест для array: заполнение части массива
- [ ] Тест для map: проверка выброса ошибки
- [ ] Тест для set: проверка выброса ошибки
- [ ] Тест для object: проверка выброса ошибки
- [ ] Тест для primitive: проверка выброса ошибки

**Генерация событий**:
- [ ] fill: при успешном заполнении
- [ ] change: при любом изменении
- [ ] set: для каждого измененного элемента

### 4.4. Реализация композитных методов модификации

#### 4.4.1. [ ] merge(source)
- [ ] **Определение**: Объединение с другим источником.
- [ ] **Сигнатура**: `merge(source) => Association`

**Применимость по типам**:
- ✅ array: arr.push(...source)
- ✅ map: source.forEach((v,k) => map.set(k,v))
- ✅ set: source.forEach(v => set.add(v))
- ✅ object: Object.assign(obj, source)
- ❌ primitive: не применимо
- ❌ weakmap: требует обсуждения
- ❌ weakset: требует обсуждения

**Тестирование**:
- [ ] Тест для array: объединение с другим массивом
- [ ] Тест для map: объединение с другой картой
- [ ] Тест для set: объединение с другим множеством
- [ ] Тест для object: объединение с другим объектом
- [ ] Тест для primitive: проверка выброса ошибки
- [ ] Тест для weakmap: после согласования
- [ ] Тест для weakset: после согласования

**Генерация событий**:
- [ ] merge: при успешном объединении
- [ ] change: при любом изменении
- [ ] set/add: для каждого добавленного элемента

#### 4.4.2. [ ] replace(newValue)
- [ ] **Определение**: Замена текущего значения.
- [ ] **Сигнатура**: `replace(newValue) => Association`

**Применимость по типам**:
- ✅ array: arr.splice(0, arr.length, ...newValue)
- ✅ map: map.clear(); newValue.forEach((v,k) => map.set(k,v))
- ✅ set: set.clear(); newValue.forEach(v => set.add(v))
- ✅ object: Object.keys(obj).forEach(k => delete obj[k]); Object.assign(obj, newValue)
- ❌ primitive: не применимо
- ❌ weakmap: требует обсуждения
- ❌ weakset: требует обсуждения

**Тестирование**:
- [ ] Тест для array: замена массива
- [ ] Тест для map: замена карты
- [ ] Тест для set: замена множества
- [ ] Тест для object: замена объекта
- [ ] Тест для primitive: проверка выброса ошибки
- [ ] Тест для weakmap: после согласования
- [ ] Тест для weakset: после согласования

**Генерация событий**:
- [ ] replace: при успешной замене
- [ ] change: при любом изменении
- [ ] clear: при очистке коллекции
- [ ] set/add: для каждого добавленного элемента

### 4.5. Тестирование и документация
- [ ] Написать `sets.test.js` для проверки каждого метода на каждом из типов данных
- [ ] Проверить корректность генерации событий для каждого метода
- [x] Написать `sets.benchmark.js` для измерения производительности
- [x] Создать документацию `SETS.md` с описанием всех методов и примерами использования
- [ ] Обновить README.md, добавив ссылку на документацию методов модификации
- [x] Обновить `benchmarkify.js` для поддержки `sets.benchmark.js`

## 5. Реализация методов работы с множествами many.js many.test.js many.benchmark.js MANY.md

### 5.1. Анализ и проектирование
- [ ] Изучить стандартные методы Set в JavaScript и их реализацию
- [ ] Определить оптимальные алгоритмы для работы с различными типами множеств (Set, Array, Object и др.)
- [ ] Спроектировать интерфейс методов для обеспечения универсальности

### 5.2. Реализация основных операций над множествами

#### 5.2.1. difference(otherSet)
- [x] Возвращает новое множество с элементами, которые есть в текущем множестве, но отсутствуют в otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.2. intersection(otherSet)
- [x] Возвращает новое множество с элементами, которые присутствуют и в текущем множестве, и в otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.3. symmetricDifference(otherSet)
- [x] Возвращает новое множество с элементами, которые есть только в одном из множеств (текущем или otherSet)
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.4. union(otherSet)
- [x] Возвращает новое множество со всеми элементами из обоих множеств (текущего и otherSet)
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Обеспечить генерацию событий при изменениях

#### 5.2.5. isDisjointFrom(otherSet)
- [x] Проверяет, что текущее множество и otherSet не имеют общих элементов
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Оптимизировать для быстрой проверки без создания промежуточных множеств

#### 5.2.6. isSubsetOf(otherSet)
- [x] Проверяет, что все элементы текущего множества содержатся в otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Оптимизировать для быстрой проверки без создания промежуточных множеств

#### 5.2.7. isSupersetOf(otherSet)
- [x] Проверяет, что текущее множество содержит все элементы otherSet
- [x] Реализовать для всех поддерживаемых типов с учетом их особенностей
- [x] Оптимизировать для быстрой проверки без создания промежуточных множеств

### 5.3. Интеграция с классом Association
- [x] Добавить методы работы с множествами в `Association._proxy` в `index.js`
- [x] Обеспечить доступность методов через экземпляры Association
- [ ] Реализовать корректную генерацию событий для каждой операции

### 5.4. Тестирование и документация
- [x] Написать `many.test.js` для проверки каждого метода работы с множествами
- [x] Написать `many.benchmark.js` для измерения производительности
- [x] Создать документацию `MANY.md` с описанием всех методов и примерами использования
- [x] Обновить README.md, добавив ссылку на документацию методов работы с множествами
- [x] Обновить `benchmarkify.js` для поддержки `many.benchmark.js`

## 6. Реализация ассоциативных релейшенов и жизненного цикла

### 6.1. Анализ и проектирование
- [x] Определить концепцию ассоциативных релейшенов
- [x] Спроектировать механизмы взаимодействия между ассоциациями
- [x] Разработать концепцию жизненного цикла ассоциаций

### 6.2. Расширение класса Association для управления жизненным циклом (кажется уже реализовано lifelycle)
- [ ] Добавить статическое свойство `all` (Map) для хранения всех созданных ассоциаций
- [ ] Модифицировать конструктор для вызова глобального события `new` при создании ассоциации
- [ ] Автоматически добавлять `this.this` (оригинальный предмет, не обертку) в `all` Set при инициализации
- [ ] Реализовать генерацию предупреждений, если добавление уже происходило
- [ ] Добавить метод `kill()`, доступный даже у проксированного экземпляра
- [ ] Метод `kill()` должен:
  - [ ] Генерировать глобальное событие `kill`
  - [ ] Генерировать персональное событие `kill` у экземпляра
  - [ ] Удалять экземпляр из `Association._all`
  - [ ] Генерировать предупреждение, если удаление уже происходило
- [ ] Обеспечить корректное удаление всех связей при вызове `kill()`

### 6.3. Реализация файла relations.js как куратора работы с Memory
- [ ] Создать файл `relations.js`
- [ ] Объявить и экспортировать экземпляры Memory для ассоциативных связей:
  - [ ] `froms` - для хранения отношений источников
  - [ ] `tos` - для хранения отношений назначений
  - [ ] `types` - для хранения типов ассоциаций
  - [ ] `values` - для хранения значений ассоциаций
- [ ] Использовать эти экземпляры Memory для реализации всех релейшенов
- [ ] Обеспечить связь с системой событий для отслеживания изменений в релейшенах

### 6.4. Доработка класса Memory
- [x] Расширить конструктор Memory, добавив опцию `childSetFactory`
- [ ] Опция `childSetFactory` должна быть методом-генератором для создания дочерних множеств (для обратных ссылок)
- [x] Модифицировать метод `many()`, чтобы он использовал `childSetFactory` при создании множества
- [ ] Реализовать возможность создания дочерних множеств как экземпляров Association
- [ ] Обеспечить автоматическую генерацию событий `change` при изменениях в множествах
- [x] Сохранить обратную совместимость с текущим API Memory
- [ ] Добавить обработку краевых случаев и ошибок

### 6.5. Реализация базовых релейшенов через Memory

#### 6.5.1. from
- [ ] Указывает на источник ассоциации
- [ ] Реализовать методы доступа и модификации через `froms` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `from`

#### 6.5.2. to
- [ ] Указывает на назначение ассоциации
- [ ] Реализовать методы доступа и модификации через `tos` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `to`

#### 6.5.3. type
- [ ] Указывает на тип ассоциации
- [ ] Реализовать методы доступа и модификации через `types` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `type`

#### 6.5.4. value
- [ ] Указывает на значение ассоциации
- [ ] Реализовать методы доступа и модификации через `values` Memory
- [ ] При изменении генерировать события в соответствующих ассоциациях и у самой ассоциации событие `value`
- [ ] Обеспечить преобразование строк обратно в значение через `.value`

#### 6.5.5. typed
- [ ] Получение всех ассоциаций с указанным типом
- [ ] Реализовать методы доступа через `types` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

#### 6.5.6. valued
- [ ] Получение всех ассоциаций с указанным значением
- [ ] Реализовать методы доступа через `values` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

#### 6.5.7. out
- [ ] Указывает на все ассоциации, для которых текущая ассоциация является источником
- [ ] Реализовать методы доступа через `froms` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

#### 6.5.8. in
- [ ] Указывает на все ассоциации, для которых текущая ассоциация является назначением
- [ ] Реализовать методы доступа через `tos` Memory (обратная связь)
- [ ] Возвращать ассоциативное множество, генерирующее события при изменениях

## 7. Реализация отслеживания связей между ассоциациями (track.js)

### 7.1. Введение и концепция

В проекте Deep необходим механизм для автоматического отслеживания взаимосвязей между ассоциациями, особенно при создании новых ассоциаций на основе существующих. Этот механизм позволит методам из gets.js возвращать не просто примитивы, а полноценные ассоциации, которые "помнят" свое происхождение и могут реагировать на изменения в родительских ассоциациях.

Принцип работы:
1. Когда одна ассоциация (дочерняя) создается на основе другой (родительской), дочерняя получает ссылку на родительскую в своем объекте `temp` под ключом `origin`
2. В дочерней ассоциации регистрируется прокси-геттер и сеттер с именем `origin`, который обеспечивает доступ к родительской ассоциации
3. Все методы из gets.js (map, filter, reduce и т.д.) модифицируются, чтобы возвращать не примитивы, а ассоциации с установленными связями с исходными данными
4. Дополнительно регистрируется геттер `track`, который возвращает экземпляр ассоциации `Track`, отслеживающий отношения между исходными и производными данными

Пример работы с обновленной системой:
```javascript
// Создаем исходную ассоциацию
const source = deep([1, 2, 3, 4]);

// Применяем метод map, создающий новую ассоциацию
const result = source.map(x => x * 2); // result.this == [2, 4, 6, 8]

// result автоматически имеет доступ к исходной ассоциации через геттер origin
console.log(result.origin === source); // true
console.log(result.origin.this); // [1, 2, 3, 4]

// Доступ к объекту трекера для отслеживания изменений
const tracker = result.track;
console.log(tracker.method); // "map"
console.log(typeof tracker.transformer); // "function"

// При изменении source, изменения автоматически отражаются в result
source.push(5);
console.log(result.this); // [2, 4, 6, 8, 10]

// При уничтожении родительской ассоциации, связь разрывается корректно
source.kill();
```

Эта система отношений обеспечит следующие преимущества:
1. Автоматическое поддержание согласованности между связанными данными
2. Возможность определить происхождение данных и их взаимосвязи
3. Оптимизация обновления данных — изменяются только затронутые элементы
4. Корректная обработка жизненного цикла ассоциаций (отписка от событий при уничтожении)

### 7.2. Реализация класса Track и механизма отслеживания

#### 7.2.1. Создание файла track.js
- [x] Определить класс `Track`, наследующий от `Association`
- [x] Реализовать конструктор, принимающий родительскую ассоциацию, дочернюю ассоциацию и функцию-трансформер
- [x] Добавить методы для работы с событиями родительской ассоциации

#### 7.2.2. Реализация механизма регистрации origin и track
- [x] Модифицировать методы в gets.js, чтобы они возвращали ассоциации вместо примитивов
- [x] Добавить регистрацию свойства `origin` в `temp` объекте дочерней ассоциации
- [x] Реализовать прокси-геттер для `origin` с использованием модели (ass, op, args)
- [x] Реализовать прокси-геттер для `track` с использованием модели (ass, op, args)

#### 7.2.3. Модификация методов gets.js для поддержки origin и track
- [x] Изменить все методы в gets.js (map, filter, reduce и т.д.), чтобы они:
  - Создавали новую ассоциацию вместо возврата примитива
  - Устанавливали `origin` на исходную ассоциацию
  - Сохраняли функцию-трансформер в `temp.transformer`

#### 7.2.4. Реализация механизма отслеживания изменений
- [x] Создать систему подписки на события родительской ассоциации
- [x] Реализовать обработчики для различных событий (set, delete, add, remove и т.д.)
- [x] Разработать логику интеллектуального обновления только затронутых элементов
- [x] Обеспечить корректное поведение при уничтожении (kill) родительской или дочерней ассоциации

### 7.4. Разделение обязанностей между модулями

#### 7.4.1. Роль track.js
- Определение ассоциации Track, наследующийся от Association
- Реализация логики подписки на события и обновления данных
- Обработка специфических событий от origin и target

#### 7.4.2. Роль gets.js
- Возвращение ассоциаций вместо примитивов
- Установка origin, transformer и method в объекте temp дочерних ассоциаций
- Поддержка цепочек вызовов методов (chaining)
- Обеспечение совместимости с существующим кодом

#### 7.4.3. Роль association.js
- Предоставление базовой инфраструктуры для работы с прокси
- Обеспечение механизма кеширования функций и свойств в объекте temp
- Поддержка обработки различных операций (get, set, apply)
- Обеспечение корректной работы с жизненным циклом ассоциаций

#### 7.4.4. Роль index.js
- Регистрация глобальных геттеров origin и track в Association._proxy
- Инициализация необходимых модулей и объектов
- Обеспечение корректного экспорта всех необходимых компонентов
- Интеграция с существующими механизмами events.js и memory.js

#### 7.4.5. Координация между модулями
- **track.js ↔ association.js**: Track использует механизмы Association для работы с прокси
- **track.js ↔ gets.js**: gets.js создает объекты, которые будут использовать Track
- **track.js ↔ events.js**: Track подписывается на события origin для обновления target
- **gets.js ↔ association.js**: gets.js создает экземпляры Association и настраивает их

- [x] Создать файл документации TRACK.md
- [x] Написать документацию по системе отслеживания изменений
- [x] Написать `track.benchmark.js` для измерения производительности
- [x] Обновить `benchmarkify.js` для поддержки `track.benchmark.js`
