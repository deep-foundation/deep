- [ ] Единообразная событийность и ассоциативность
    - [ ] достичь повсеместного единообразия системы событий, все операции над множествами покрыты тестами и предсказуемо единообразно их вызывают
    - [ ] deep.select позволяет искать по ссылочному пространству и подписываться на события изменений результатов поиска, выдавая события в корректном порядке
    - проконтролировать побочные эффекты:
        - проверить отсутствие утечек памяти при многократной подписке и отписке от событий
        - обеспечить корректную работу с циклическими ссылками в графе объектов
        - гарантировать, что изменения в одной части графа не вызывают неожиданных событий в других частях
    - создать бенчмарки для:
        - измерения производительности deep.select при работе с большими графами объектов
        - сравнения потребления памяти до и после оптимизации системы событий
        - оценки скорости распространения событий при различных сценариях использования
    - разработать тесты для проверки:
        - порядка событий при одновременных изменениях в графе объектов
        - корректности работы deep.select при динамическом изменении графа
        - единообразия событий для всех типов операций над множествами (add, delete, clear, etc.)
    - определить и документировать:
        - стандартный формат событий для всех операций
        - правила порядка событий при сложных изменениях
        - рекомендации по эффективной работе с системой событий