{
  "project": {
    "name": "deep@7.2.0"
  },
  "architecture": {
    "folders": {
    },
    "files": {
      "**/*.experiment.js": "Ручные эксперименты запускаемые через node ./*.experiment.js. Эти файлы не попадут в git и npm.",
      "**/*.test.js": "Проверка конечного стабильного функционала. Тесты, только проверка стабильных реализаций, честно отвечает на вопрос реализованности проекта. Тесты всегда должны ипользовать safe методы (без _ префикса). На один файл функционала [name].js приходится один файл [name].test.js. Запускай тесты только так NODE_OPTIONS='--no-inspect' npm test",
      "*.benchmark.js": "На один файл функционала [name].js приходится один файл [name].benchmark.js. Генерация отчетов в формате Markdown происходит автоматически при запуске бенчмарков. Бенчмарки можно запустить командой npm run benchmark.",
      "*.md": "Документация в корне. На один файл функционала [name].js приходится один файл [NAME].md с документацией (название в верхнем регистре, а расширение - в нижнем). Каждый md-файл должен содержать заголовок # Производительность после первого заголовка и описания."
    }
  },
  "aiConfig": {
    "language": "ru-RU"
  },
  "persistence": {
    "reminders": {
      "interval": 1
    }
  },
  "dependencies": {
    "preference": "Предпочтение отдается встроенным пакетам Node.js (node:test, node:assert и др.), сторонние пакеты используются только когда необходимо"
  }
}
